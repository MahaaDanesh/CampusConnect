# CampusConnect — Frontend

React + Vite + Tailwind CSS single-page app. See the [root README](../README.md) for full setup and deployment instructions.

```bash
npm install
cp .env.example .env   # set VITE_API_URL
npm run dev
```
